/**
 * @file stub.cpp
 * @brief Placeholder translation unit for minimal CMake targets.
 * @details Kept for compatibility with build configurations that expect at
 *          least one concrete source file in the root `src` directory.
 */

#include "novaphy/novaphy.h"
