/**
 * @file body.cpp
 * @brief Translation unit for rigid body core types.
 * @details Most `Body` methods are defined inline in the header for minimal
 *          overhead in hot simulation paths.
 */
#include "novaphy/core/body.h"
